generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model CardDetails {
  id              String @id @default(cuid())
  cardholderName  String
  cardNumber      String
  cvc             String
  expirationMonth Int
  expirationYear  Int
}
